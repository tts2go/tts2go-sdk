{
  "name": "tts2go-sdk",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "turbo run build",
    "release": "pnpm build && pnpm -r publish --no-git-checks"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
